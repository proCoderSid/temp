import { AssignTemplateDirective } from './assign-template.directive';

describe('AssignTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new AssignTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
