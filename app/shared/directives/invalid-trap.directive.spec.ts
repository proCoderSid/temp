import { InvalidTrapDirective } from './invalid-trap.directive';

describe('InvalidTrapDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidTrapDirective();
    expect(directive).toBeTruthy();
  });
});
