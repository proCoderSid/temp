import { OnEnterNextElementDirective } from './on-enter-next-element.directive';

describe('OnEnterNextElementDirective', () => {
  it('should create an instance', () => {
    const directive = new OnEnterNextElementDirective();
    expect(directive).toBeTruthy();
  });
});
