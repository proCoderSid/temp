import { UniqueValidatorDirective } from './unique-validator.directive';

describe('UniqueValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
